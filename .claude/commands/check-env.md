Check that all required environment variables are present without revealing their values. Read the `.env` file and verify the following keys exist and are non-empty: PM_USERNAME, PM_PASSWORD, SUPABASE_URL, SUPABASE_KEY, TELEGRAM_BOT_TOKEN, SCOUT_BOT_TOKEN, TELEGRAM_CHAT_ID. Report any missing or empty variables. Also check that `web/.env.local` exists with NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.
